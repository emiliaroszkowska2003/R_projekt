analizuj_zarobki <- function(dane, stanowisko) {
  # Tu w przyszłości wpiszemy kod filtrujący dane
  return(paste("Statystyki dla stanowiska:", stanowisko))
}

porownaj_doswiadczenie <- function(dane) {
  # Tu w przyszłości wpiszemy kod grupujący dane
  return("Tabela porównawcza doświadczenia")
}
#test git
